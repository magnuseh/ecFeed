package com.testify.ecfeed.parsers;

import com.testify.ecfeed.model.PartitionNode;

public class RandomModelGenerator {
	public PartitionNode generatePartitionNode(int depth){
		String partitionNameRegex = "dupa";
		
		PartitionNode partitionNode = new PartitionNode("name", 0);
		
		return partitionNode;
	}
}
